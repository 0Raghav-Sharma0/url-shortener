<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL Shortener | Home</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    :root {
      --primary: #4361ee;
      --primary-light: #4895ef;
      --secondary: #3f37c9;
      --dark: #1b263b;
      --light: #f8f9fa;
      --success: #4cc9f0;
      --danger: #f72585;
      --warning: #f8961e;
      --gray: #adb5bd;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    }

    body {
      background-color: #f5f7ff;
      color: var(--dark);
      line-height: 1.6;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 2rem;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
      padding-bottom: 1rem;
      border-bottom: 1px solid rgba(0, 0, 0, 0.1);
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--primary);
    }

    .logo i {
      font-size: 1.8rem;
    }

    .nav-links {
      display: flex;
      gap: 1.5rem;
    }

    .nav-links a {
      text-decoration: none;
      color: var(--dark);
      font-weight: 500;
      transition: color 0.3s;
    }

    .nav-links a:hover {
      color: var(--primary);
    }

    .btn {
      padding: 0.6rem 1.2rem;
      border-radius: 0.5rem;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.3s;
      border: none;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
    }

    .btn-primary {
      background-color: var(--primary);
      color: white;
    }

    .btn-primary:hover {
      background-color: var(--secondary);
      transform: translateY(-2px);
    }

    .btn-outline {
      background-color: transparent;
      border: 1px solid var(--primary);
      color: var(--primary);
    }

    .btn-outline:hover {
      background-color: var(--primary);
      color: white;
    }

    .hero {
      text-align: center;
      padding: 3rem 0;
      margin-bottom: 3rem;
    }

    .hero h1 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      color: var(--dark);
    }

    .hero p {
      font-size: 1.1rem;
      color: var(--gray);
      max-width: 600px;
      margin: 0 auto 2rem;
    }

    .url-form {
      background-color: white;
      padding: 2rem;
      border-radius: 1rem;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
      margin-bottom: 3rem;
      max-width: 800px;
      margin-left: auto;
      margin-right: auto;
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-group label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: 500;
    }

    .form-control {
      width: 100%;
      padding: 0.8rem 1rem;
      border: 1px solid #ddd;
      border-radius: 0.5rem;
      font-size: 1rem;
      transition: border-color 0.3s;
    }

    .form-control:focus {
      outline: none;
      border-color: var(--primary);
    }

    .result-box {
      background-color: #f8f9fa;
      padding: 1rem;
      border-radius: 0.5rem;
      margin-top: 1rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .result-box p {
      margin: 0;
      font-family: 'Courier New', monospace;
      color: var(--primary);
    }

    .copy-btn {
      background-color: var(--primary);
      color: white;
      border: none;
      padding: 0.3rem 0.8rem;
      border-radius: 0.3rem;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    .copy-btn:hover {
      background-color: var(--secondary);
    }

    .urls-table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 2rem;
      background-color: white;
      border-radius: 0.5rem;
      overflow: hidden;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
    }

    .urls-table th, .urls-table td {
      padding: 1rem;
      text-align: left;
    }

    .urls-table th {
      background-color: var(--primary);
      color: white;
      font-weight: 500;
    }

    .urls-table tr:nth-child(even) {
      background-color: #f8f9fa;
    }

    .urls-table tr:hover {
      background-color: #f1f3ff;
    }

    .short-url {
      color: var(--primary);
      text-decoration: none;
      font-weight: 500;
    }

    .short-url:hover {
      text-decoration: underline;
    }

    .clicks-badge {
      background-color: var(--success);
      color: white;
      padding: 0.2rem 0.5rem;
      border-radius: 1rem;
      font-size: 0.8rem;
      font-weight: 500;
    }

    footer {
      text-align: center;
      margin-top: 3rem;
      padding: 1.5rem 0;
      color: var(--gray);
      font-size: 0.9rem;
      border-top: 1px solid rgba(0, 0, 0, 0.1);
    }

    @media (max-width: 768px) {
      .container {
        padding: 1rem;
      }
      
      header {
        flex-direction: column;
        gap: 1rem;
      }
      
      .nav-links {
        gap: 1rem;
      }
      
      .hero h1 {
        font-size: 2rem;
      }
      
      .url-form {
        padding: 1.5rem;
      }
      
      .urls-table {
        font-size: 0.9rem;
      }
      
      .urls-table th, .urls-table td {
        padding: 0.8rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <div class="logo">
        <i class="fas fa-link"></i>
        <span>Short.ly</span>
      </div>
      <nav class="nav-links">
        <a href="#">Home</a>
        <a href="#">Features</a>
        <a href="#">Pricing</a>
        <a href="/user/login">Login</a>
        <a href="/user/signup" class="btn btn-outline">Sign Up</a>
      </nav>
    </header>

    <main>
      <section class="hero">
        <h1>Shorten Your Links, Amplify Your Reach</h1>
        <p>Create short, memorable URLs and track their performance with our powerful analytics dashboard.</p>
      </section>

      <section class="url-form">
        <form method="POST" action="/url">
          <div class="form-group">
            <label for="url">Enter your long URL</label>
            <input type="text" name="url" id="url" class="form-control" placeholder="https://example.com/very-long-url-that-needs-to-be-shortened" required>
          </div>
          <button type="submit" class="btn btn-primary">
            <i class="fas fa-magic"></i> Shorten URL
          </button>
        </form>

        <% if (locals.id) { %>
        <div class="result-box">
          <p>http://localhost:8001/url/<%= id %></p>
          <button class="copy-btn" onclick="copyToClipboard('http://localhost:8001/url/<%= id %>')">
            <i class="far fa-copy"></i> Copy
          </button>
        </div>
        <% } %>
      </section>

      <% if (locals.urls) { %>
      <section class="url-history">
        <h2>Your Shortened URLs</h2>
        <table class="urls-table">
          <thead>
            <tr>
              <th>#</th>
              <th>Short URL</th>
              <th>Original URL</th>
              <th>Clicks</th>
            </tr>
          </thead>
          <tbody>
            <% urls.forEach((url, index) => { %>
            <tr>
              <td><%= index + 1 %></td>
              <td>
                <a href="/url/<%= url.shortId %>" class="short-url" target="_blank">
                  /<%= url.shortId %>
                </a>
              </td>
              <td style="max-width: 300px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">
                <%= url.redirectURL %>
              </td>
              <td>
                <span class="clicks-badge"><%= url.visitHistory.length %></span>
              </td>
            </tr>
            <% }) %>
          </tbody>
        </table>
      </section>
      <% } %>
    </main>

    <footer>
      <p>&copy; 2023 Short.ly - All rights reserved</p>
    </footer>
  </div>

  <script>
    function copyToClipboard(text) {
      navigator.clipboard.writeText(text).then(() => {
        alert('URL copied to clipboard!');
      }).catch(err => {
        console.error('Failed to copy: ', err);
      });
    }
  </script>
</body>
</html>